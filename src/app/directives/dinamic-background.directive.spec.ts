import { DinamicBackgroundDirective } from './dinamic-background.directive';

describe('DinamicBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new DinamicBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
